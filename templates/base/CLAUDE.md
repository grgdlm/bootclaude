# Project: {{PROJECT_NAME}}

> Generated by `/bootstrap` on {{DATE}}. Stack: {{STACK_SUMMARY}}.
> Edit freely — this file is the single source of truth Claude loads every session.

## What this project is

<!-- One or two sentences. What does this app do, who is it for? Fill this in. -->

## Golden rules

- Prefer TypeScript everywhere it applies; no `any` unless justified with a comment.
- Match existing patterns in the file you're editing before introducing new ones.
- Small, reviewable changes. If a task needs many files, propose a plan first.
- Never commit secrets. `.env*` files are git-ignored and unreadable by Claude by design.
- Run the project's checks (typecheck, lint, tests) before declaring a task done.

## Commands

<!-- Fill in the real commands for this repo. These are the defaults the tooling assumes. -->

- Install: `{{INSTALL_CMD}}`
- Dev: `{{DEV_CMD}}`
- Build: `{{BUILD_CMD}}`
- Test: `{{TEST_CMD}}`
- Lint / format: `{{LINT_CMD}}`

## Conventions

- Package manager: {{PKG_MANAGER}}
- Formatting is enforced by a PostToolUse hook — don't hand-format; let it run.

<!-- Stack-specific guidance is appended below by the bootstrap overlays. -->
