# Project: {{PROJECT_NAME}}

> Generated by `/bootstrap` on {{DATE}}. This is a multi-repo project workspace.
> This file is an index. Each sub-repo has its own CLAUDE.md with stack-specific detail.

## What this project is

{{PROJECT_DESCRIPTION}}

## Repositories

{{SUBPROJECTS}}

## Working across repos

- Each folder above is its own git repo. Commit inside the repo you are editing.
- When a change spans repos (an API contract, a shared type), update both and keep them in
  sync in the same session.
- Run build, test, and dev commands inside the specific repo, not from this root.
